from django.forms import ModelForm
from appLeoClimb.models import devinciclimbingcontest

""" class InscriptionForm(ModelForm):
    class Meta:
        model = devinciclimbingcontest
        fields = ''
           CHOICES = (
        (1, "S"),
        (1, "M"),
        (1, "L"),
        (1, "XL")
    )
    tshirt = models.ChoiceField(choices=CHOICES)

    QUESTION = (
        (1, "Reseaux"),
        (1, "Asso de votre école"),
        (1, "Entourage"),
        (1, "Autres")
    )
    question = models.ChoiceField(choices=QUESTION) """